[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm db:generate", "pnpm db:migrate:prod", "pnpm build"]

[start]
cmd = "node dist/main.js"
